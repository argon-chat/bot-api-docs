---
import IntersectThree from 'phosphor-astro/IntersectThreeDuotone.astro';

interface Props {
  class?: string;
  size?: number;
}
const { class: className = '', size = 20 } = Astro.props;
---

<IntersectThree class={className} width={size} height={size} />
